// This component is no longer used in the new layout.
// The functionality has been moved to the top toolbar in App.tsx.
import React from 'react';

export const Toolbox: React.FC = () => {
  return null;
};
